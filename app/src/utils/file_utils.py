
# треба поміняти щоб воно лише повертало,  а не зверталось до папки і рендерило сторінку
def save_result_csv():
    pass
    #залишу як приклад того що було
    #filename = f"result_{uuid.uuid4().hex}.csv"
    #file_path = os.path.join(settings.OUT_DIR, filename)
   #with open(file_path, "w", encoding="utf-8-sig") as f:
    #    f.write(content)
    #return file_path